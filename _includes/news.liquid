<div class="news">
  {% if site.news != blank %}
    {% assign news_size = site.news | size %}
    <div
      class="news-timeline-wrapper{% if include.limit and page.announcements.scrollable and news_size > 3 %} news-timeline-scrollable{% endif %}"
      {% if include.limit and page.announcements.scrollable and news_size > 3 %}
        style="max-height: 60vw"
      {% endif %}
    >
      {% assign news = site.news | reverse %}
      {% if include.limit and page.announcements.limit %}
        {% assign news_limit = page.announcements.limit %}
      {% else %}
        {% assign news_limit = news_size %}
      {% endif %}
      <ol class="news-timeline">
        {% for item in news limit: news_limit %}
          <li class="news-timeline-item">
            <time class="news-timeline-date" datetime="{{ item.date | date_to_xmlschema }}">
              {{ item.date | date: '%b %d, %Y' }}
            </time>
            <div class="news-timeline-content">
              {% if item.inline %}
                {{ item.content | remove: '<p>' | remove: '</p>' | emojify }}
              {% else %}
                <a class="news-title" href="{{ item.url | relative_url }}">{{ item.title }}</a>
              {% endif %}
            </div>
          </li>
        {% endfor %}
      </ol>
    </div>
  {% else %}
    <p>No news so far...</p>
  {% endif %}
</div>
